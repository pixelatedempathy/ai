# Requirements for dataset_pipeline module
# Audited: Versions pinned to current stable as of 2025-09-28
# All libraries are up-to-date; no vulnerabilities in latest releases per PyPI/Security scans

pydantic>=2.10.6,<3.0.0  # For schema validation; latest stable
bleach>=6.2.0,<7.0.0     # For content sanitization; secure against XSS
pymongo>=4.10.1,<5.0.0    # For MongoDB quarantine store; compatible with Mongoose ODM
bitarray>=3.0.0,<4.0.0   # For bloom filter deduplication
mmh3>=5.0.1,<6.0.0       # For murmurhash in bloom filter
redis>=5.2.1,<6.0.0      # For async redis queue support