apiVersion: v1
kind: Service
metadata:
  name: {{ include "pixel-voice.fullname" . }}
  labels:
    {{ include "pixel-voice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{ include "pixel-voice.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
